# Blocked subagent types — one per line, format: name:sandbox
# name: matched case-insensitively, hyphens and underscores are interchangeable
# sandbox: the codex sandbox mode Claude should use instead
#
# To add a new blocked subagent, append a line here and run:
#   bash scripts/sync-hooks.sh
test_gen:workspace-write
doc_comments:workspace-write
diff_digest:read-only
